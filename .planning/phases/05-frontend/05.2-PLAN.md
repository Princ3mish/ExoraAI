---
wave: 2
depends_on: [05-PLAN]
files_modified: []
autonomous: true
---

# Phase 05 Wave 2: Authentication Pages & Dashboard Shell

<task>
<read_first>
- frontend/package.json
</read_first>

<action>
Install supplementary Shadcn UI components for Layout interfaces.
1. Run `npx shadcn@latest add dropdown-menu avatar sheet -y` to grab components critical for our mobile-first sidebar and sleek Header Profile Dropdown.
</action>

<acceptance_criteria>
- Terminal executes component injection successfully.
</acceptance_criteria>
</task>

<task>
<read_first>
- frontend/src/context/AuthContext.tsx
</read_first>

<action>
Construct Login and Register interfaces with immersive styling.
1. Create `frontend/src/pages/Login.tsx` rendering a centered Framer Motion card. Use Shadcn Inputs. Implement onSubmit using Context `login` callback.
   - If success, push to `/dashboard`.
   - If error, display via Top Toast notification.
2. Create `frontend/src/pages/Register.tsx` similarly mirroring Login with 'name', 'email', 'password'.
</action>

<acceptance_criteria>
- `Login.tsx` and `Register.tsx` files are defined with responsive UI code and functional data binding.
</acceptance_criteria>
</task>

<task>
<read_first>
- frontend/src/context/AuthContext.tsx
</read_first>

<action>
Build the Dashboard Architecture Shell (Header and Sidebar).
1. Create `frontend/src/components/layout/Sidebar.tsx` mapping router navigation links (Overview, Meetings, Calendar). Note: use Lucide icons dynamically.
2. Create `frontend/src/components/layout/Header.tsx` hosting a User Avatar dropdown containing "Logout" functionality linked to Context API.
3. Create `frontend/src/components/layout/DashboardLayout.tsx` managing layout orchestration. Feature standard Sidebars on Desktop, and utilize ShadCN `Sheet` for responsive mobile navigation.
</action>

<acceptance_criteria>
- Layout structural components exist. Sidebar scales dynamically off mobile and uses standard React Router Link nodes.
</acceptance_criteria>
</task>

<task>
<read_first>
- frontend/src/routes/AppRoutes.tsx
</read_first>

<action>
Connect Wave 2 Pages to AppRouter.
1. Inside `frontend/src/routes/AppRoutes.tsx`, delete the mock placeholders.
2. Import `Login`, `Register` from `pages`.
3. Import `DashboardLayout` from `components/layout/DashboardLayout`.
4. Ensure protected paths resolve exactly to DashboardLayout.
</action>

<acceptance_criteria>
- Routes are structurally mapped without mock inline arrays.
</acceptance_criteria>
</task>
