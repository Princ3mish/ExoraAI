---
wave: 3
depends_on: [05.2-PLAN]
files_modified: []
autonomous: true
---

# Phase 05 Wave 3: Admin UI Dashboards

<task>
<read_first>
- frontend/package.json
</read_first>

<action>
Install supplementary Shadcn UI components for Data interfaces.
1. Run `npx shadcn@latest add table dialog select popover calendar badge -y` specifically for the heavily data-driven Tables and Date pickers required for the Admin Meeting forms.
</action>

<acceptance_criteria>
- Terminal executes component injection successfully.
</acceptance_criteria>
</task>

<task>
<read_first>
- frontend/src/routes/AppRoutes.tsx
</read_first>

<action>
Construct generic Dashboard root overview.
1. Create `frontend/src/pages/dashboard/DashboardOverview.tsx`.
2. Construct statistical Card elements showcasing "Total Meetings", "Active AI Prompts", etc as native static UI placeholders using the `lucide-react` icons and ShadCN `Card` layout.
</action>

<acceptance_criteria>
- File constructed and functionally responsive.
</acceptance_criteria>
</task>

<task>
<read_first>
- backend/prisma/schema.prisma
</read_first>

<action>
Build the core Meeting List Component.
1. Create `frontend/src/pages/dashboard/MeetingsList.tsx`. 
2. Use ShadCN `Table` element. 
3. Connect it locally to `apiClient.get('/meetings')` utilizing a `useEffect` loop. 
4. Implement standard Shadcn `Badge` for status arrays. Add a "Create Meeting" CTA button which maps strictly to `/dashboard/meetings/new`.
</action>

<acceptance_criteria>
- MeetingsList parses backend context into Table grids.
</acceptance_criteria>
</task>

<task>
<read_first>
- frontend/src/pages/dashboard/MeetingsList.tsx
</read_first>

<action>
Build Meeting Creation Form.
1. Create `frontend/src/pages/dashboard/CreateMeeting.tsx`.
2. Provide ShadCN `Input` fields mapped to Backend attributes: Title, Description, Duration, TimeRange, and Participant arrays.
3. Submit native array to `apiClient.post('/meetings')`. Execute `use-toast` throwing success logs, then pop react-router back to `/dashboard/meetings`.
</action>

<acceptance_criteria>
- Creation form correctly parses data payloads and commits to DB.
</acceptance_criteria>
</task>

<task>
<read_first>
- frontend/src/routes/AppRoutes.tsx
</read_first>

<action>
Mount Dashboard internal routes into the shell wrapper.
1. Hook `DashboardOverview`, `MeetingsList`, and `CreateMeeting` to `<Route>` blocks encapsulated inside `DashboardLayout` wrapper to populate the `<Outlet />`.
</action>

<acceptance_criteria>
- Links natively resolve.
</acceptance_criteria>
</task>
