---
wave: 2
depends_on: [04-PLAN]
files_modified:
  - backend/src/services/ai.service.js
  - backend/src/api/ai/ai.schema.js
  - backend/prisma/schema.prisma
  - backend/test/api/ai.test.js
autonomous: true
---

# Phase 4.2: AI Robustness & Persistence Layers

<task>
<read_first>
- backend/src/api/ai/ai.schema.js
- backend/src/services/ai.service.js
</read_first>

<action>
Implement explicit Zod output validation schemas in `backend/src/api/ai/ai.schema.js`.
1. Export `suggestTimesOutputSchema`, `draftEmailOutputSchema`, and `summaryOutputSchema`.
2. `suggestTimesOutputSchema`: object with `suggestions` array containing objects with `start` (string, datetime), `end` (string, datetime), and `score` (number).
3. `draftEmailOutputSchema`: object with `subject` (string) and `body` (string).
4. `summaryOutputSchema`: object with `summary` (string), `bulletPoints` (array of strings), `actionItems` (array of objects with owner, task, deadline), and `decisions` (array of strings).
5. Open `backend/src/services/ai.service.js` and import these new Zod schemas. Replace the custom `RESPONSE_VALIDATORS` manual checking logic inside `validateAIResponse` with Zod `.safeParse()`.
</action>

<acceptance_criteria>
- `backend/src/api/ai/ai.schema.js` exports `suggestTimesOutputSchema`, `draftEmailOutputSchema`, and `summaryOutputSchema`.
- `backend/src/services/ai.service.js` uses Zod for validating output structure in `validateAIResponse`.
</acceptance_criteria>
</task>

<task>
<read_first>
- backend/src/services/ai.service.js
</read_first>

<action>
Implement an Orchestrator-Level Global Timeout & Normalization Layer in `backend/src/services/ai.service.js`.
1. Wrap the entire `provider.fn(user, system)` call block with a `Promise.race` that throws a `ProviderError('Orchestrator Timeout', { type: 'TIMEOUT' })` after 15,000ms.
2. In the `validateAIResponse` step (after Zod validation succeeds), add a Normalization Layer.
3. For `TASK_TYPES.SUGGEST_TIMES`: Map over the valid `data.suggestions`, forcing `start` and `end` fields through `new Date().toISOString()` to guarantee strict ISO string formats.
4. For `TASK_TYPES.DRAFT_EMAIL`: Sanitize `data.body` by removing any potential trailing XML/markdown artifacts from the LLM.
</action>

<acceptance_criteria>
- `backend/src/services/ai.service.js` contains a `Promise.race` injecting a 15s global timeout across any provider execution.
- `backend/src/services/ai.service.js` contains a normalization block mutating/sanitizing `data.suggestions` dates and `data.body` strings before returning them in `validateAIResponse`.
</acceptance_criteria>
</task>

<task>
<read_first>
- backend/src/services/ai.service.js
</read_first>

<action>
Add Pre-Rate-Limit Quota Checks in `backend/src/services/ai.service.js`.
1. Add a lightweight rolling token/request quota counter variable in memory `let globalTokenEstimate = 0; const QUOTA_LIMIT = 50000;`.
2. Before calling `provider.fn()`, check if `globalTokenEstimate > QUOTA_LIMIT`. If so, dynamically restrict fallback logic or throw a rate limit error early.
3. After a successful AI call, append `result.usage.total_tokens` (or an approximate value like 500 if undefined) to `globalTokenEstimate`.
4. Reset `globalTokenEstimate` to 0 every hour using a global interval or time-based timestamp diffing.
</action>

<acceptance_criteria>
- `backend/src/services/ai.service.js` contains `globalTokenEstimate` and `QUOTA_LIMIT` variables.
- `globalTokenEstimate` is checked before dispatching requests and updated with token counts afterward.
</acceptance_criteria>
</task>

<task>
<read_first>
- backend/prisma/schema.prisma
</read_first>

<action>
Add Database Persistence for AI summaries/suggestions in `backend/prisma/schema.prisma`.
1. Open `backend/prisma/schema.prisma`.
2. Add a new model `AIResult`:
   - `id` (String, @id, @default(uuid()))
   - `meetingId` (String)
   - `taskType` (String)
   - `payload` (Json)
   - `createdAt` (DateTime, @default(now()))
   - Define relation to `Meeting` model (`meeting   Meeting @relation(fields: [meetingId], references: [id], onDelete: Cascade)`).
3. In the `Meeting` model, add `aiResults AIResult[]`.
</action>

<acceptance_criteria>
- `backend/prisma/schema.prisma` contains the `AIResult` model.
- `Meeting` model has a relation to `AIResult` defined.
</acceptance_criteria>
</task>

<task>
<read_first>
- backend/prisma/schema.prisma
- backend/src/services/ai.service.js
- package.json
</read_first>

<action>
Implement the Persistence Logic and update tests.
1. Run `npx prisma db push --accept-data-loss` to sync the new schema to the DB.
2. In `backend/src/services/ai.service.js`, import `PrismaClient` and instantiate it.
3. Before returning a successful AI request from `generateCompletion`, conditionally save the `data` to the `AIResult` table if a `meetingId` is present in the `metadata`.
4. Update `backend/test/api/ai.test.js` to either mock `prisma.aIResult.create` or ensure it executes without failing the assertions. Ensure all 29 tests pass via `npm test`.
</action>

<acceptance_criteria>
- `backend/src/services/ai.service.js` invokes `prisma.aIResult.create({})` on successful completion.
- Running `npm test -- --testPathPattern="ai.test"` exits with 0 (All tests pass).
</acceptance_criteria>
</task>
